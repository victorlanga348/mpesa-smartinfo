# Temporary Agent Flow

1. Evento `temporary-agent:requested` pode ser emitido.
2. Servidor repassa para mapa/admin.
3. Evento `temporary-agent:accepted` pode ser emitido.
4. Evento `temporary-agent:available` pode inserir agente no mapa.

## Estado

Sem API e sem persistencia.

