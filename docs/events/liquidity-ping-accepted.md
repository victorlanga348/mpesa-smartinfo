# ping:accepted

Emitido quando agente aceita ping ou status vira `ACCEPTED`.

## Origem

- `PUT /api/ping/:id/accept`
- `PUT /api/ping/:id/status`

## Destino

`client:{userId}` e `agent:{agentId}`.

